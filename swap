#include <stdio.h> 
int main() 
{ 
    int x = 8, y = 4; 
  
    // Code to swap 'x' and 'y' 
    x = x * y; 
    y = x / y; 
    x = x / y; 
  
    printf("After Swapping: x = %d, y = %d", x, y); 
  
    return 0; 
} 
